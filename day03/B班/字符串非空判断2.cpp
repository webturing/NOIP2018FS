#include<bits/stdc++.h>

using namespace std;

int main() {
	string s="";
	if(s.size() == 0) {
		cout << s << " is empty." << endl;
	} else {
		cout << s << " isnt empty. length:"<< s.size() << endl;
	}
	
	//s.size()和s.length()的使用功能相同，都是求该字符串的长度 
	return 0;
}
